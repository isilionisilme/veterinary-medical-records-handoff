# Confidence policy configuration for veterinarian-facing mapping confidence bands.
# Keep low_max < mid_max and both values within [0, 1].
VET_RECORDS_CONFIDENCE_POLICY_VERSION=v1-local
VET_RECORDS_CONFIDENCE_LOW_MAX=0.50
VET_RECORDS_CONFIDENCE_MID_MAX=0.75
